# kubectl apply -f manifests/debug.yml
# kubectl delete -f manifests/debug.yml

apiVersion: v1
kind: Pod
metadata:
  name: debug
spec:
  containers:
  - name: debug
    image: dersimn/netutils:ubuntu
    imagePullPolicy: IfNotPresent
    command:
      - sleep
      - infinity
    volumeMounts:
      # - name: backup
      #   mountPath: /mnt/backup
      # - name: config
      #   mountPath: /mnt/config
      - name: nix-1
        mountPath: /mnt/nix-1
      - name: nix-2
        mountPath: /mnt/nix-2
  volumes:
    # - name: backup
    #   persistentVolumeClaim:
    #     claimName: nas-backup
    # - name: config
    #   persistentVolumeClaim:
    #     claimName: cross-seed-config-deluge-vpn-0
    - name: nix-1
      persistentVolumeClaim:
        claimName: dc-backup-nix-builder
    - name: nix-2
      persistentVolumeClaim:
        claimName: wl-dns-nix-builder
