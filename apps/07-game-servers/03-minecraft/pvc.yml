apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-cobblemon-overclocked
  namespace: games
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
