apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deluge-theme
  namespace: media
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
